on:
  pull_request:
  push:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - run: apt install -y libphysfs-dev
      - uses: actions/checkout@v2
      - uses: iffy/install-nim@v4
      - run: nimble install -y
      - run: nimble test